name: wechat_assets_picker
version: 8.0.0
publish_to: none

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

  extended_image:
   path: ./extended_image
  photo_manager: ^2.2.0
  provider: ^6.0.2
  video_player: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
